# Corpus sweep reconcile (no AI)

_Candidates diffed against the live catalog only; the notes are not judged. `set` rows would still be re-judged by a full sweep — that is what catches wrong existing values._

| bucket | rows |
| --- | ---: |
| empty | 119 |
| **total** | **119** |

