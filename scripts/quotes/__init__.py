"""Quote checking over the cached sources a ``cite:`` points at.

:mod:`quotes.sources` resolves a cite to its source text; :mod:`quotes.verbatim`
is the substring gate built on it. Whether the quote *supports* the claim is the
other half, and lives in ``ai_lint.quote_support``.
"""
